Instruction for codex to pick-up progress:
***
Continue from current `media-metadata-explorer` workspace state. Do not discard unstaged changes.

Current unstaged files:
- README.md
- backlog.txt
- src/photo_archive.egg-info/PKG-INFO
- src/photo_archive/database.py
- src/photo_archive/normalize.py
- tests/test_normalize.py
- tests/test_reporting.py
- tests/test_ffprobe_extractor.py (new)
- tests/test_scanner.py (new)

Goal:
1) Finish/verify the latest video-metadata stabilization edits.
2) Run tests and fix any failures.
3) Run end-to-end scan/report validation.
4) Summarize results and propose commit message.

Known context:
- ffprobe-based video extraction was added earlier.
- Latest edits included:
  - DB scan-history backward-compatible fallback in `database.py`
  - video timestamp priority fix in `normalize.py` (`ffprobe:creation_time` before filesystem fallback)
  - new tests for scanner/ffprobe/normalize/reporting
  - README updates for ffprobe requirements and media extraction stats

Commands to run (Windows, from repo root):
1) Activate env + install deps
   - .\.venv\Scripts\activate
   - python -m pip install -e ".[dev,ui]"

2) Verify tools
   - exiftool -ver
   - ffprobe -version
   If ffprobe is missing:
   - winget install --id Gyan.FFmpeg --exact --accept-package-agreements --accept-source-agreements
   - reopen terminal and re-run `ffprobe -version`

3) Fast checks
   - python -m compileall -q src tests
   - python -m pytest -q tests/test_scanner.py tests/test_ffprobe_extractor.py tests/test_normalize.py tests/test_reporting.py

4) Full tests
   - python -m pytest -q

5) End-to-end validation
   - python -m photo_archive.cli scan "C:\Users\rmeer\Pictures" --db-path data/db/photo_archive.duckdb
   - python -m photo_archive.cli report --db-path data/db/photo_archive.duckdb
   - python -m photo_archive.cli scan "C:\Users\rmeer\Pictures" --db-path data/db/photo_archive.duckdb
   - python -m photo_archive.cli report --db-path data/db/photo_archive.duckdb

Expected report output:
- “Media extraction stats”
- image/video attempted/successful/failed counts

At the end:
- list what passed/failed
- provide minimal patch for any failures
- provide final commit message


