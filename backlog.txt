Instruction for codex to pick-up progress:
***
Ik was bezig met back-up functionaliteit: Herkennen welke bestanden nog niet gekopieerd zijn.

Dit moet nog gererund:

	You’re absolutely right: that output is still wrong for your real data.

	Root cause was a bug in full-rescan behavior:

	unchanged files were being re-extracted/hashed, but not persisted for backup-audit matching.
	I fixed that now in pipeline.py and added regression tests in test_pipeline.py.

	Run this next:

	python -m pytest tests/test_pipeline.py tests/test_exiftool_extractor.py tests/test_hash_utils.py tests/test_backup_audit.py tests/test_database.py tests/test_reporting.py
	Then refresh primary hashes (one-time long run):

	python -m photo_archive.cli scan "C:\Users\rmeer\Pictures" --db-path data/db/photo_archive.duckdb --full-rescan
	Then rerun audit:

	python -m photo_archive.cli backup-audit --db-path data/db/photo_archive.duckdb --primary-root "C:\Users\rmeer\Pictures" --backup-root "E:\Pictures" --limit 200
	After this, primary_hashed_files should jump up and backed_up_files should no longer be 0.