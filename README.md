# media-metadata-explorer
A local-first tool to scan media files, extract and normalize metadata (EXIF, GPS, device, timestamps), and store it in a queryable database. Enables filtering, search, and visualization (timeline, map, gallery) to bring structure to large photo and video collections.
